minimum_cumulusci_version: '3.90.0'
project:
    name: Safely
    package:
        name: Safely
        api_version: '62.0'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
